# 量化选股前端设计文档

本文档描述前端页面和交互设计，供 Vue 前端开发使用。

## 技术栈建议

- **框架**: Vue 3 + Composition API
- **UI库**: Element Plus 或 Ant Design Vue
- **图表**: ECharts（K线图）
- **HTTP**: Axios
- **路由**: Vue Router
- **状态管理**: Pinia（可选）

---

## API 基础信息

- **Base URL**: `http://localhost:8000`
- **文档**: `http://localhost:8000/docs` (Swagger UI)

---

## 页面设计

### 1. 首页 / 预测大盘 (`/`)

主页面，展示某一天的所有预测结果。

#### 布局

```
┌─────────────────────────────────────────────────────────────┐
│  Header: Logo + 导航菜单 + 日期选择器                          │
├─────────────────────────────────────────────────────────────┤
│  统计卡片区                                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 预测总数  │ │ 看涨数量  │ │ 准确率    │ │ 热门股票  │       │
│  │   156    │ │   42     │ │  68.5%   │ │ 贵州茅台  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│  筛选工具栏                                                   │
│  [得分阈值滑块: 0.5 ━━━━━━━━ 0.95]  [只看预测为1] [搜索框]      │
├─────────────────────────────────────────────────────────────┤
│  预测结果表格                                                  │
│  ┌────────┬────────┬────────┬────────┬────────┬────────┐   │
│  │ 代码   │ 名称    │ 得分   │ 预测   │ 实际收益 │ 状态   │   │
│  ├────────┼────────┼────────┼────────┼────────┼────────┤   │
│  │ 600519 │ 贵州茅台│ 0.92   │ 看涨 ↑ │ +6.2%  │ ✓ 正确 │   │
│  │ 000858 │ 五粮液 │ 0.87   │ 看涨 ↑ │ +3.1%  │ ✗ 错误 │   │
│  │ ...    │ ...    │ ...    │ ...    │ ...    │ ...    │   │
│  └────────┴────────┴────────┴────────┴────────┴────────┘   │
│  分页: [< 1 2 3 4 5 >]                                       │
└─────────────────────────────────────────────────────────────┘
```

#### 功能

1. **日期选择器**: 选择查看哪天的预测
2. **得分阈值滑块**: 动态筛选得分 ≥ 阈值的股票
3. **预测标签筛选**: 只看预测为1（看涨）的
4. **表格排序**: 点击列头按得分/收益排序
5. **点击行**: 跳转到股票详情页
6. **执行预测按钮**: 对当前日期重新预测（调用POST接口）

#### API 调用

```javascript
// 获取预测列表
GET /api/predict/{date}?min_score=0.5&prediction=1&limit=50&offset=0

// 执行预测
POST /api/predict
Body: { "date": "2026-01-15", "threshold": 0.5 }

// 获取统计
GET /api/stats/accuracy
```

---

### 2. 股票详情页 (`/stock/:code`)

展示单只股票的详细信息和K线图。

#### 布局

```
┌─────────────────────────────────────────────────────────────┐
│  Header + 返回按钮                                           │
├─────────────────────────────────────────────────────────────┤
│  股票信息卡片                                                 │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  600519 贵州茅台                                         ││
│  │  最新价: ¥1856.00  涨跌: +2.35%                          ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  K线图区域                                                   │
│  [日K线] [5分钟]  时间范围: [近30天 ▼]                        │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                                                         ││
│  │              📈 K线图 (ECharts)                          ││
│  │                                                         ││
│  │         蜡烛图 + 成交量柱状图                             ││
│  │                                                         ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  预测历史                                                    │
│  ┌────────┬────────┬────────┬────────┬────────┐            │
│  │ 日期   │ 得分    │ 预测   │ 实际   │ 结果   │            │
│  ├────────┼────────┼────────┼────────┼────────┤            │
│  │ 01-15 │ 0.92   │ 看涨   │ +6.2% │ ✓      │            │
│  │ 01-14 │ 0.45   │ 看跌   │ -1.2% │ ✓      │            │
│  └────────┴────────┴────────┴────────┴────────┘            │
└─────────────────────────────────────────────────────────────┘
```

#### 功能

1. **K线切换**: 日K线 / 5分钟K线
2. **时间范围**: 近7天/30天/60天/120天
3. **K线图交互**: 缩放、拖动、十字光标
4. **预测历史表格**: 该股票的历史预测记录

#### API 调用

```javascript
// 获取股票信息
GET /api/stocks/{code}

// 获取日K线
GET /api/kline/{code}/daily?limit=60

// 获取5分钟K线
GET /api/kline/{code}/5min?limit=480

// 获取该股票的预测历史（需遍历predictions）
GET /api/predict/{date}/{code}
```

---

### 3. 搜索页 (`/search`)

股票搜索页面。

#### 布局

```
┌─────────────────────────────────────────────────────────────┐
│  搜索框（大）                                                 │
│  ┌─────────────────────────────────────────────────────────┐│
│  │  🔍  输入股票代码或名称...                                ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  搜索结果                                                    │
│  ┌──────────────────────────────────────────┐              │
│  │  600519  贵州茅台                    →   │              │
│  ├──────────────────────────────────────────┤              │
│  │  000858  五粮液                      →   │              │
│  ├──────────────────────────────────────────┤              │
│  │  002304  洋河股份                    →   │              │
│  └──────────────────────────────────────────┘              │
│                                                             │
│  热门搜索: 贵州茅台 | 宁德时代 | 比亚迪 | 中国平安            │
└─────────────────────────────────────────────────────────────┘
```

#### 功能

1. **实时搜索**: 输入时自动搜索（防抖 300ms）
2. **点击结果**: 跳转到股票详情页
3. **热门搜索**: 显示热门股票标签

#### API 调用

```javascript
// 模糊搜索
GET /api/stocks/search?q=茅台&limit=20
```

---

### 4. 统计页 (`/stats`)

模型预测准确率统计。

#### 布局

```
┌─────────────────────────────────────────────────────────────┐
│  整体统计                                                    │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐       │
│  │ 总预测数  │ │ 正确数   │ │ 准确率   │ │ 精确率   │       │
│  │  12,456  │ │  8,234  │ │  66.1%  │ │  72.3%  │       │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘       │
├─────────────────────────────────────────────────────────────┤
│  准确率趋势图                                                │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                    📈 折线图                             ││
│  │         x轴: 日期  y轴: 准确率                           ││
│  └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│  每日统计表格                                                │
│  ┌────────┬────────┬────────┬────────┬────────┐            │
│  │ 日期   │ 总数   │ 看涨数  │ 已验证  │ 准确率  │            │
│  ├────────┼────────┼────────┼────────┼────────┤            │
│  │ 01-15 │ 156   │ 42     │ 156    │ 68.5%  │            │
│  │ 01-14 │ 148   │ 38     │ 148    │ 65.2%  │            │
│  └────────┴────────┴────────┴────────┴────────┘            │
└─────────────────────────────────────────────────────────────┘
```

#### API 调用

```javascript
// 获取统计数据
GET /api/stats/accuracy

// 获取热门预测
GET /api/stats/top?date=2026-01-15&limit=10
```

---

## 组件设计

### 通用组件

| 组件 | 说明 |
|------|------|
| `AppHeader` | 顶部导航栏，包含Logo、菜单、搜索入口 |
| `DatePicker` | 日期选择器，支持快捷选项 |
| `StockCard` | 股票卡片，显示代码、名称、价格 |
| `ScoreSlider` | 得分阈值滑块 |
| `PredictionBadge` | 预测标签（看涨/看跌） |
| `ResultBadge` | 结果标签（正确✓/错误✗/待验证） |

### K线图组件

使用 ECharts 实现，配置示例：

```javascript
// ECharts K线图配置
const option = {
  tooltip: { trigger: 'axis' },
  legend: { data: ['K线', '成交量'] },
  grid: [
    { left: '10%', right: '10%', height: '50%' },
    { left: '10%', right: '10%', top: '65%', height: '20%' }
  ],
  xAxis: [
    { type: 'category', data: dates, gridIndex: 0 },
    { type: 'category', data: dates, gridIndex: 1 }
  ],
  yAxis: [
    { scale: true, gridIndex: 0 },
    { scale: true, gridIndex: 1 }
  ],
  series: [
    {
      name: 'K线',
      type: 'candlestick',
      data: klineData,  // [[open, close, low, high], ...]
      itemStyle: {
        color: '#ef5350',      // 涨
        color0: '#26a69a',     // 跌
        borderColor: '#ef5350',
        borderColor0: '#26a69a'
      }
    },
    {
      name: '成交量',
      type: 'bar',
      xAxisIndex: 1,
      yAxisIndex: 1,
      data: volumeData
    }
  ]
};
```

---

## 路由设计

```javascript
const routes = [
  { path: '/', name: 'Home', component: HomePage },
  { path: '/stock/:code', name: 'StockDetail', component: StockDetailPage },
  { path: '/search', name: 'Search', component: SearchPage },
  { path: '/stats', name: 'Stats', component: StatsPage }
];
```

---

## API 接口汇总

### 预测相关

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/predict` | 执行预测 |
| GET | `/api/predict/{date}` | 获取预测列表 |
| GET | `/api/predict/{date}/{code}` | 获取预测详情 |

### 股票相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stocks/search?q=xxx` | 模糊搜索 |
| GET | `/api/stocks/{code}` | 股票详情 |
| GET | `/api/stocks` | 股票列表 |

### K线相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/kline/{code}/daily` | 日K线 |
| GET | `/api/kline/{code}/5min` | 5分钟K线 |

### 统计相关

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/stats/accuracy` | 准确率统计 |
| GET | `/api/stats/daily/{date}` | 每日统计 |
| GET | `/api/stats/top?date=xxx` | 热门预测 |

---

## 数据格式示例

### 预测列表响应

```json
{
  "date": "2026-01-15",
  "total": 156,
  "positive_count": 42,
  "items": [
    {
      "code": "600519",
      "name": "贵州茅台",
      "score": 0.92,
      "prediction": 1,
      "actual_return": 0.062,
      "actual_label": 1,
      "is_correct": true
    }
  ]
}
```

### K线数据响应

```json
{
  "code": "600519",
  "name": "贵州茅台",
  "freq": "daily",
  "data": [
    {
      "datetime": "2026-01-10",
      "open": 1820.0,
      "high": 1865.0,
      "low": 1815.0,
      "close": 1856.0,
      "volume": 12345678,
      "amount": 22876543210
    }
  ]
}
```

---

## 颜色规范

| 用途 | 颜色 | 色值 |
|------|------|------|
| 涨/看涨 | 红色 | `#ef5350` |
| 跌/看跌 | 绿色 | `#26a69a` |
| 预测正确 | 绿色 | `#4caf50` |
| 预测错误 | 红色 | `#f44336` |
| 主色调 | 蓝色 | `#1976d2` |
| 背景色 | 浅灰 | `#f5f5f5` |

---

## 响应式设计

- **桌面端**: 表格展示，K线图大尺寸
- **移动端**: 卡片列表展示，K线图自适应宽度

---

## 启动后端

```bash
cd D:/deep_learning/trading
python run_api.py

# 访问文档
http://localhost:8000/docs
```
